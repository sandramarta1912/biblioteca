extends ../../layout.pug

block body
    .container-fluid
        .card.admin
            .card-body.border.border-black.rounded
                h1.text-center Predicatori
                table.table.table-striped
                    thead
                        tr
                            th Nume
                            th Varsta
                            th Predici
                            if typeof preachers != 'undefined' && preachers.length > 0
                                each preacher, index in preachers
                                    tr
                                        td #{preacher.name}
                                        td #{preacher.author}